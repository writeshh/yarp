<?php

declare(strict_types=1);

namespace {{ namespace }};

use {{ interfaceFqcn }};
use {{ modelFqcn }};
use Writeshh\Yarp\BaseRepository;

/**
 * @extends BaseRepository<{{ model }}>
 */
class {{ class }} extends BaseRepository implements {{ interface }}
{
    public function __construct({{ model }} $model)
    {
        parent::__construct($model);
    }

    /*
     * Add query methods specific to {{ model }} here. Inside the repository you
     * have the fluent constraint API and, for anything it does not cover,
     * $this->newQuery() for a raw Eloquent builder.
     *
     * public function active(): \Illuminate\Database\Eloquent\Collection
     * {
     *     return $this->where('active', true)->latest()->get();
     * }
     */
}
